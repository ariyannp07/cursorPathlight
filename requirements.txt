# =============================================================================
# PATHLIGHT REQUIREMENTS - JETSON ORIN NANO OPTIMIZED
# JetPack 6.2.1 | CUDA 12.6 | Ubuntu 22.04 | Python 3.10
# =============================================================================

# --- Core Vision (YOLO via PyTorch; OpenCV built from source, NOT pip) ---
ultralytics==8.3.0         # YOLOv8 - tested with NumPy 1.26.4
numpy==1.26.4              # Pinned for YOLOv8 8.3.0 compatibility
Pillow==10.4.0             # Image processing

# NOTE: OpenCV is built from source with CUDA/GStreamer support
# DO NOT pip install opencv-python - it lacks CUDA/CSI camera support

# --- Face Recognition Stack ---
# IMPORTANT: Build dlib from source with CUDA/cuDNN FIRST, then install face_recognition
face-recognition==1.3.0    # Face detection and recognition

# Fast vector search for face embeddings (lighter than scikit-learn)
hnswlib==0.8.0             # High-performance ANN library

# --- Audio I/O + TTS/ASR Stack ---
pyttsx3==2.90              # Text-to-speech
SpeechRecognition==3.10.0  # Speech recognition
pyaudio==0.2.14            # Audio I/O (requires system PortAudio)
librosa==0.10.1            # Audio analysis
numba==0.59.1              # Numba for librosa acceleration
llvmlite==0.42.0           # LLVM binding for numba
soundfile==0.12.1          # Audio file I/O

# --- AI Assistant and Language Processing ---
openai>=1.0.0              # OpenAI API for AI assistant
# transformers==4.41.2     # Optional: Only if local HF models needed (memory-heavy on Orin Nano)

# --- Navigation and Path Planning ---
networkx==3.2.1           # Graph algorithms for path planning
scipy==1.11.4             # Scientific computing
matplotlib>=3.7.0         # Plotting and visualization

# --- Hardware Control ---
smbus2==0.4.3             # I2C communication
spidev>=3.6               # SPI communication

# --- Web and API Integration ---
requests==2.32.3          # HTTP requests
PyYAML==6.0.1             # YAML configuration files
googlemaps>=4.10.0        # Google Maps API
fastapi>=0.100.0          # Web API framework
uvicorn>=0.23.0           # ASGI server

# --- Utilities ---
python-dotenv>=1.0.0      # Environment variable management
click>=8.1.0              # Command line interface
tqdm>=4.65.0              # Progress bars
loguru>=0.7.0             # Advanced logging

# --- Testing ---
pytest>=7.4.0            # Testing framework
pytest-cov>=4.1.0        # Coverage testing
pytest-asyncio>=0.21.0   # Async testing support

# --- Development ---
black>=23.0.0             # Code formatting
flake8>=6.0.0             # Code linting
mypy>=1.5.0               # Type checking

# =============================================================================
# INSTALLATION NOTES:
# 
# 1. Install system dependencies FIRST:
#    sudo apt install portaudio19-dev libsndfile1 espeak-ng gstreamer1.0-*
#
# 2. Install PyTorch/TorchVision for Jetson BEFORE this requirements.txt:
#    - torch==2.5.0 (CUDA 12.6 Jetson wheel)
#    - torchvision==0.19.1 (source built)
#
# 3. Build OpenCV from source with CUDA/GStreamer (NOT pip)
#
# 4. Build dlib from source with CUDA BEFORE installing face-recognition
#
# 5. Then: pip install -r requirements.txt
# ============================================================================= 